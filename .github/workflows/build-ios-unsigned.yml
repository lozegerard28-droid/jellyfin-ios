name: Build iOS unsigned IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Patch boost podspec (jfrog discontinued -> archives.boost.io)
        run: |
          find node_modules/react-native -name "boost.podspec" -exec sed -i '' 's|boostorg\.jfrog\.io/artifactory/main|archives.boost.io|g' {} +
          grep -rl "boostorg.jfrog" node_modules/react-native || echo "OK: aucune URL jfrog restante"

      - name: Set up Ruby 3.1
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1
          bundler-cache: true

      - name: CocoaPods install
        run: |
          cd ios
          bundle exec pod install --repo-update

      - name: Xcode archive (unsigned)
        run: |
          xcodebuild archive \
            -workspace ios/Jellyfin.xcworkspace \
            -scheme Jellyfin \
            -configuration Release \
            -archivePath output/Jellyfin.xcarchive \
            CODE_SIGNING_ALLOWED=NO CODE_SIGN_STYLE=Manual

      - name: Export IPA (unsigned)
        run: |
          xcodebuild -exportArchive \
            -archivePath output/Jellyfin.xcarchive \
            -exportPath output/ \
            -exportOptionsPlist exportOptions.plist

      - uses: actions/upload-artifact@v4
        with:
          name: Jellyfin-ipa
          path: output/Jellyfin.ipa
